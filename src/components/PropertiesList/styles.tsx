import styled from "styled-components";

const StyledPropertiesList = styled.div`
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  column-gap: 35px;
  row-gap: 50px;
`;

export { StyledPropertiesList };
